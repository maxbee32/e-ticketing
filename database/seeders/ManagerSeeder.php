<?php

namespace Database\Seeders;

use App\Models\Manager;
use Illuminate\Database\Seeder;
use Illuminate\Database\Console\Seeds\WithoutModelEvents;

class ManagerSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //
        // Manager::factory()->count(1)
        // ->create()->each(
        //     function($user) {
        //         $user->assignRole('Enterance Manager');
        //         // Assign roles to users (in this case for user id -> 1 & 2)
        // // Manager::find(1)->assignRole('Enterance Manager');
        // // Manager::find(2)->assignRole('Restaurant Manager');
        // // Manager::find(3)->assignRole('Admin');
        //     }
        // );
    }
}
